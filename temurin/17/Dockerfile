# AUTO-GENERATED by scripts/generate_dockerfiles.py - DO NOT EDIT BY HAND.
# Regenerate with: python3 scripts/generate_dockerfiles.py
#
# Base image:      eclipse-temurin:17-jre-resolute
# Output image:    ghcr.io/esempe-id/eclipse-temurin:17-jre
# Extra packages:  (none)

FROM eclipse-temurin:17-jre-resolute

LABEL org.opencontainers.image.source="https://github.com/esempe-id/seed"
LABEL org.opencontainers.image.title="Esempe runner image (Temurin 17 JRE)"
LABEL org.opencontainers.image.description="Temurin 17 JRE with runner system packages"
LABEL org.opencontainers.image.version="17-jre"
LABEL org.opencontainers.image.ref.name="ghcr.io/esempe-id/eclipse-temurin:17-jre"

# Install packages as root (the temurin base image already runs as root).
USER root

# Install the standard runner packages plus the extras from package.extras.
RUN apt-get update \
    && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
        bash \
        ca-certificates \
        curl \
        jq \
        unzip \
        wget \
    && rm -rf /var/lib/apt/lists/*

