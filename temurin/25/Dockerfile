# AUTO-GENERATED by scripts/generate_dockerfiles.py - DO NOT EDIT BY HAND.
# Regenerate with: python3 scripts/generate_dockerfiles.py
#
# Base image:      eclipse-temurin:25-jre-resolute
# Output image:    ghcr.io/esempe-id/eclipse-temurin:25-jre
# Extra packages:  (none)

FROM eclipse-temurin:25-jre-resolute

LABEL org.opencontainers.image.source="https://github.com/esempe-id/seed"
LABEL org.opencontainers.image.title="Esempe runner image (Temurin 25 JRE)"
LABEL org.opencontainers.image.description="Temurin 25 JRE with runner system packages"
LABEL org.opencontainers.image.version="25-jre"
LABEL org.opencontainers.image.ref.name="ghcr.io/esempe-id/eclipse-temurin:25-jre"

# Install packages as root (the temurin base image already runs as root).
USER root

# Install the standard runner packages plus the extras from package.extras.
RUN apt-get update \
    && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
        bash \
        ca-certificates \
        curl \
        jq \
        unzip \
        wget \
    && rm -rf /var/lib/apt/lists/*

